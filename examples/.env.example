# Copy this file to `.env` (same directory) and fill in the keys you need.
# The `.env` file is gitignored — your secrets stay local.
#
# The example preflight loads this automatically via python-dotenv.

# OpenAI — https://platform.openai.com/api-keys
OPENAI_API_KEY=

# Anthropic — https://console.anthropic.com/
ANTHROPIC_API_KEY=